{
  "hooks": {
    "Stop": [
      {
        "type": "command",
        "command": "npm test || exit 2",
        "windows": "npm test; if ($LASTEXITCODE -ne 0) { exit 2 }",
        "timeout": 300
      }
    ]
  }
}
